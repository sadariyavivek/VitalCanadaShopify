{% comment %}
  The contents of the page.offers.liquid template can be found in /sections/page-offers-template.liquid
{% endcomment %}
  <div aria-label="Navigation menu breadcrumb" role="navigation" class="breadcrumb_text center" itemscope itemtype="http://schema.org/BreadcrumbList">
    <span itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem"><a aria-label="Breadcrumb link for {{ 'general.breadcrumbs.home' | t }}" href="{{ shop.url }}" title="{{ shop.name | escape }}" itemprop="item" class="breadcrumb_link"><span itemprop="name">{{ 'general.breadcrumbs.home' | t }}</span></a></span>
    <span class="breadcrumb-divider">/</span>
    <span itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
      {% if page %}
        <a href="{{ page.url }}" aria-current="page" title="{{ page.title | escape }}" aria-label="Breadcrumb link for {{ page.title }}" itemprop="item" class="breadcrumb_link shop-link"><span itemprop="name">{{ page.title }}</span></a>

      {% endif %}
    </span>
  </div>
</div>
{% section 'page-subscription-perks-template' %}