{% layout none %}
{
	{% for item in cart.items %}
		"{{ item.variant_id }}": {
			"bold_group_id": "{{ item.variant.metafields.bold_rp.rp_group_id }}"
		}
		{% unless forloop.last %},{% endunless %}
	{% endfor %}
}